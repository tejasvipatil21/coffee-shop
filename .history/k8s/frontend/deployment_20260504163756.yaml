apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: coffee-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
        image: coffee-frontend
        ports:
        - containerPort: 80